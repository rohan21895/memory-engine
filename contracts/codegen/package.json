{
  "name": "@memory-engine/contracts-codegen",
  "version": "0.0.0",
  "private": true,
  "description": "Generates Python, TypeScript and Rust bindings from contracts/schemas",
  "scripts": {
    "generate": "python3 generate.py",
    "generate:check": "python3 generate.py --check"
  }
}
