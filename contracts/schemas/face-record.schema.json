{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://memory-engine.local/contracts/v0/face-record.schema.json",
  "title": "FaceRecord",
  "description": "One detected face in one frame: where it is, what it looks like as an embedding, which cluster it fell into, and -- separately and much more cautiously -- which person we are willing to say it is.",
  "$comment": "PRECISION OVER RECALL (CLAUDE.md hard rule 5). A wrong person in a family album is a catastrophic failure, so identity is deliberately split into three independent things: `cluster` (unsupervised, cheap, may be wrong), `identity.person_id` (a claim), and `identity.eligible_for_automated_output` (permission to act on the claim). Only the third gates unattended output, and it is false unless a human confirmed the assignment or the model cleared the high-confidence threshold. Everything uncertain goes to the review queue rather than into an album.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "face_id",
    "media_id",
    "detection",
    "identity",
    "sensitive"
  ],
  "properties": {
    "schema_version": {
      "$ref": "common.schema.json#/$defs/SchemaVersion"
    },
    "face_id": {
      "description": "BLAKE3 over (media_id, frame_time, quantised bbox, detector model_id + version). Content-addressed, so re-running the same detector on the same frame produces the same id and re-detection is idempotent. Changing detector version deliberately produces new ids rather than silently mutating old ones.\n\nCANONICAL ENCODING (issue #34), because 'BLAKE3 over the tuple' does not determine the bytes and every writer picked its own. The hashed byte string is exactly:\n\n    face_id = BLAKE3( utf8( DOMAIN US media_id US TIME US BBOX US MODEL_ID US VERSION ) )\n\nwhere US is U+001F INFORMATION SEPARATOR ONE, written once between adjacent fields and nowhere else, and the six fields are:\n\nDOMAIN -- the literal ASCII string 'face:v1'. This versions THIS ENCODING, not the detector. Changing the encoding means bumping it, so a re-encoding produces new ids on purpose rather than colliding with old ones by accident.\n\nmedia_id -- the 64 lowercase hex characters, verbatim.\n\nTIME -- the EMPTY STRING when frame_time is null, i.e. for a still. Otherwise `<value>/<rate>`, each number rendered in RFC 8785 / ECMAScript Number::toString form, which is the same numeric rule edl_id already uses. So 1001 and 1001.0 both render as `1001`, and a rate of 30000/1001 renders as `29.97002997002997`. THIS IS THE FIELD THAT BREAKS FIRST ACROSS LANGUAGES: Python's repr writes `1.0` where JavaScript writes `1`, and the same frame then gets two ids. A number that cannot be rendered without an exponent is REJECTED rather than written, because exponent formatting is where the two languages stop agreeing and no real frame rate needs one. The still case cannot be confused with the video case: a rendered time always contains a `/`, and the empty string never does.\n\nBBOX -- `<qx>,<qy>,<qw>,<qh>` where q(v) = round_half_away_from_zero(v * 10000), rendered as a base-10 integer with no padding and no sign (every component is non-negative by schema, so half-away-from-zero and half-up coincide). NOT banker's rounding: Python's round() sends 3002.5 to 3002 while JavaScript's Math.round and Rust's f64::round both send it to 3003, and 8855 of the 10000 half-quantum positions in [0,1] are exactly representable as doubles, so this is reachable rather than theoretical. The quantum is 1e-4 of the frame -- 0.6px on a 6000px original, finer than any detector's own precision and coarse enough that the last-bit disagreement between two execution providers cannot turn one face into two.\n\nTHE PRODUCT IS EVALUATED IN BINARY64, NOT EXACTLY. `v * 10000` is the IEEE-754 double multiplication, correctly rounded to a double, and half-away-from-zero is then applied to THAT double. Pinning the rounding mode without pinning the arithmetic leaves two rules, not one: x = 0.00035 is stored as the double 0.000349999999999999996..., so the EXACT product is 3.4999999999999999644... and rounds to 3, while the BINARY64 product is exactly 3.5 and rounds to 4. Measured over the 10000 half-quantum decimal literals in [0,1] the two readings disagree on 4419 of them, and over two million uniformly random doubles they disagree on none -- which is why a suite built from random boxes cannot find this, and why the vector table carries `bbox-half-quantum-only-in-binary64` specifically. Binary64 is pinned because it is what a producer doing float arithmetic computes without trying, and because it is what both existing implementations already do; a third writer reaching for exact rational arithmetic to be 'more correct' would silently issue a second id for a face that already has one. `rotation_deg` does NOT participate, which is why Detection pins it to 0.\n\nMODEL_ID, VERSION -- detection.detector.model_id and .version, verbatim. model_id is a Slug and cannot contain the separator; ModelRef.version is pattern-constrained to exclude control characters for exactly this reason, so the join is injective and needs no length prefix.\n\nDELIBERATELY NOT IN THE TUPLE: weights_blake3, config_blake3, runtime, precision, detected_on, detection_score, landmarks and embedding. All of them are still RECORDED, on the detector ModelRef and in model_runs, so provenance is not lost -- but none of them may move the id. weights_blake3 is nullable in development mode, so including it would rename every face the moment the same detection is re-recorded against a pinned registry: duplicated rows where deduplication was the point. config_blake3 moves when a score threshold moves, which changes WHICH faces are found rather than the identity of one that was found -- and a config change that really does move a box further than the quantum already changes BBOX. `version` is the deliberate, human-controlled switch for 'issue new ids'.\n\ncontracts/tests recomputes this for every face fixture and for contracts/vectors/face-id.json, in Python and again in TypeScript against the generated bindings. An identity that is asserted rather than computed is how issue #26's invented span_id happened, and this is the same failure one schema over.",
      "$ref": "common.schema.json#/$defs/Blake3Hash"
    },
    "media_id": {
      "description": "The MediaRecord this face was found in. For spanned video this is the assembly record, so a face track can cross a chapter boundary.",
      "$ref": "common.schema.json#/$defs/Blake3Hash"
    },
    "frame_time": {
      "description": "Position within the video, in SOURCE time (already mapped back through the proxy frame index). Null for stills.",
      "oneOf": [
        {
          "$ref": "common.schema.json#/$defs/RationalTime"
        },
        {
          "type": "null"
        }
      ]
    },
    "track": {
      "description": "Face track membership for video. One person walking through a 4-second shot is ~120 FaceRecords sharing a track_id; the ranking and moment layers work on tracks, not frames.",
      "oneOf": [
        {
          "$ref": "#/$defs/FaceTrack"
        },
        {
          "type": "null"
        }
      ]
    },
    "detection": {
      "$ref": "#/$defs/Detection"
    },
    "landmarks": {
      "oneOf": [
        {
          "$ref": "#/$defs/Landmarks"
        },
        {
          "type": "null"
        }
      ]
    },
    "embedding": {
      "description": "Recognition embedding. Null when the face was detected but was too small, too blurred or too occluded to embed reliably -- an unembeddable face is still worth recording, because it counts toward 'how many people are in this photo'.",
      "oneOf": [
        {
          "$ref": "common.schema.json#/$defs/VectorRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "attributes": {
      "oneOf": [
        {
          "$ref": "#/$defs/FaceAttributes"
        },
        {
          "type": "null"
        }
      ]
    },
    "cluster": {
      "oneOf": [
        {
          "$ref": "#/$defs/ClusterMembership"
        },
        {
          "type": "null"
        }
      ]
    },
    "identity": {
      "$ref": "#/$defs/Identity"
    },
    "sensitive": {
      "$ref": "#/$defs/SensitiveFlags"
    },
    "model_runs": {
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/ModelRun"
      },
      "default": []
    },
    "created_at": {
      "$ref": "common.schema.json#/$defs/Timestamp"
    },
    "updated_at": {
      "$ref": "common.schema.json#/$defs/Timestamp"
    }
  },
  "allOf": [
    {
      "$comment": "A confirmed minor may be detected and counted freely, but may not be NAMED without an explicit minor_face_labeling consent. Expressed as: confirmed_minor implies labeling_consent is an object, which the SensitiveFlags constraint then forces to be correctly scoped and unrevoked. Applied whenever the status is confirmed_minor rather than only when a person_id is already attached, so the consent has to exist before the label can be written rather than being back-filled after.",
      "if": {
        "type": "object",
        "properties": {
          "sensitive": {
            "type": "object",
            "properties": {
              "minor_status": {
                "const": "confirmed_minor"
              }
            },
            "required": [
              "minor_status"
            ]
          }
        },
        "required": [
          "sensitive"
        ]
      },
      "then": {
        "properties": {
          "sensitive": {
            "type": "object",
            "required": [
              "labeling_consent"
            ],
            "properties": {
              "labeling_consent": {
                "type": "object"
              }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "Detection": {
      "title": "Detection",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "bbox",
        "detection_score",
        "detector"
      ],
      "properties": {
        "bbox": {
          "description": "Normalised against the ORIENTED frame, so the same box is valid on the 512px thumbnail the detector saw and on the 6000px original the renderer will crop.",
          "$ref": "common.schema.json#/$defs/NormalizedBox"
        },
        "detection_score": {
          "$ref": "common.schema.json#/$defs/Confidence"
        },
        "detector": {
          "$ref": "common.schema.json#/$defs/ModelRef"
        },
        "detected_on": {
          "description": "Which rendition the detector ran against. Small faces missed on a thumbnail are re-detected at full resolution on demand; recording this makes 'have we already looked properly' answerable.",
          "type": "string",
          "enum": [
            "thumbnail_512",
            "preview_2048",
            "video_proxy_480p",
            "original"
          ],
          "default": "thumbnail_512"
        },
        "face_area_ratio": {
          "description": "Fraction of the frame the box covers. The single best predictor of whether an embedding will be trustworthy, and a hard input to the automated-output threshold.",
          "$ref": "common.schema.json#/$defs/Unit"
        }
      },
      "allOf": [
        {
          "$comment": "A detection box is axis-aligned, and `rotation_deg` does not participate in face_id (see the encoding on face_id). A record carrying a non-zero rotation would therefore give two genuinely different boxes the same identity -- the id would silently collapse them, which is the exact failure mode content-addressing exists to prevent. No detector in the registry emits a rotated box; pinning it here means a writer that starts to has to change the id encoding first rather than discovering the collision in a family album. Absent is the normal case and is unconstrained by this block, because NormalizedBox already defaults it to 0.",
          "properties": {
            "bbox": {
              "properties": {
                "rotation_deg": {
                  "const": 0
                }
              }
            }
          }
        }
      ]
    },
    "Landmarks": {
      "title": "Landmarks",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "scheme",
        "points"
      ],
      "properties": {
        "scheme": {
          "description": "Point count and ordering convention. Consumers must switch on this rather than assuming an index layout. yunet_5 and insightface_5 are both five points and are NOT interchangeable: feeding one to an alignment template built for the other produces a plausible warp and a wrong embedding, which is the worst failure mode in this system because nothing downstream can detect it.",
          "type": "string",
          "enum": [
            "insightface_5",
            "insightface_106",
            "mediapipe_468",
            "yunet_5"
          ]
        },
        "points": {
          "type": "array",
          "items": {
            "$ref": "common.schema.json#/$defs/Point2D"
          },
          "minItems": 5,
          "maxItems": 468
        },
        "score": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "FaceAttributes": {
      "title": "FaceAttributes",
      "description": "Everything that decides whether this is a GOOD photo of this person, as opposed to whether it is this person. Feeds face-quality scoring, album hero selection, and the 'is anyone blinking' check.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "yaw_deg": {
          "description": "Head pose. Beyond about +/-45 degrees yaw, recognition confidence degrades sharply and the automated-output threshold should tighten.",
          "oneOf": [
            {
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            {
              "type": "null"
            }
          ]
        },
        "pitch_deg": {
          "oneOf": [
            {
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            {
              "type": "null"
            }
          ]
        },
        "roll_deg": {
          "oneOf": [
            {
              "type": "number",
              "minimum": -180,
              "maximum": 180
            },
            {
              "type": "null"
            }
          ]
        },
        "eyes_open": {
          "description": "Probability both eyes are open. The blink check that saves an album spread.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "smile": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "mouth_open": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "gaze_on_camera": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "sharpness": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Unit"
            },
            {
              "type": "null"
            }
          ]
        },
        "occlusion": {
          "description": "How much of the face is hidden by a hand, hair, mask or another person.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Unit"
            },
            {
              "type": "null"
            }
          ]
        },
        "wearing_sunglasses": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "wearing_mask": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "quality": {
          "description": "Fused face quality, the value album selection actually sorts on.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Score"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "ClusterMembership": {
      "title": "ClusterMembership",
      "description": "Unsupervised grouping over embedding distance (HDBSCAN over cosine). A cluster is a hypothesis, not an identity: it is allowed to be wrong, which is exactly why it is stored separately from `identity`.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "cluster_id",
        "method"
      ],
      "properties": {
        "cluster_id": {
          "$ref": "common.schema.json#/$defs/Uuid"
        },
        "method": {
          "type": "string",
          "enum": [
            "hdbscan_cosine",
            "agglomerative_cosine",
            "user_grouped",
            "singleton"
          ]
        },
        "membership_strength": {
          "description": "HDBSCAN membership probability. Low values sit near the decision boundary and are precisely the ones the active-learning loop should ask a human about first -- ten well-chosen taps fix a thousand photos.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Unit"
            },
            {
              "type": "null"
            }
          ]
        },
        "is_noise": {
          "description": "HDBSCAN noise point: too far from any cluster. Never surfaces in automated output.",
          "type": "boolean",
          "default": false
        },
        "distance_to_centroid": {
          "oneOf": [
            {
              "type": "number",
              "minimum": 0
            },
            {
              "type": "null"
            }
          ]
        },
        "clustering_run_id": {
          "description": "Which clustering pass produced this. Re-clustering a growing library reshuffles cluster ids; pinning the run makes the reshuffle auditable instead of mysterious.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Slug"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "Identity": {
      "title": "Identity",
      "description": "Who we say this is, how sure we are, and whether that is sure enough to act on unattended.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "assignment",
        "eligible_for_automated_output"
      ],
      "properties": {
        "person_id": {
          "description": "Null until an assignment exists. A person is a user-facing entity created by labeling, never by clustering alone.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Uuid"
            },
            {
              "type": "null"
            }
          ]
        },
        "assignment": {
          "description": "How the person_id was arrived at. `auto_below_threshold` is a real, common state: the model has a guess it is not allowed to use, which is the entire point of precision-first.",
          "type": "string",
          "enum": [
            "unassigned",
            "user_confirmed",
            "user_rejected",
            "auto_high_confidence",
            "auto_below_threshold",
            "review_queued",
            "ambiguous_multiple_candidates"
          ]
        },
        "confidence": {
          "description": "Calibrated similarity-to-person confidence. Null when unassigned.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "threshold_profile": {
          "description": "Which operating point was applied. `automated_output` is the strict one tuned for >=99% precision (build plan section 7); `search_only` is permissive because a wrong hit in a search result is a shrug, not a catastrophe.",
          "type": "string",
          "enum": [
            "automated_output",
            "review_queue",
            "search_only"
          ],
          "default": "automated_output"
        },
        "threshold_used": {
          "description": "The actual numeric threshold applied, stored so that retuning the operating point is a replayable decision rather than a silent behaviour change.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Confidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "eligible_for_automated_output": {
          "description": "THE GATE. Album, film and reel selection may only treat this face as a known person when this is true. Invariant, enforced in tests: true requires assignment to be user_confirmed, or auto_high_confidence with confidence >= threshold_used. Every other state is false.",
          "type": "boolean"
        },
        "candidates": {
          "description": "Runner-up people considered. Populated when assignment is ambiguous_multiple_candidates, which is the twins-and-siblings case that a single best-match number hides.",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "person_id",
              "confidence"
            ],
            "properties": {
              "person_id": {
                "$ref": "common.schema.json#/$defs/Uuid"
              },
              "confidence": {
                "$ref": "common.schema.json#/$defs/Confidence"
              }
            }
          },
          "maxItems": 8,
          "default": []
        },
        "review_reason": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "below_threshold",
                "near_boundary",
                "multiple_candidates",
                "new_cluster",
                "user_reported_error",
                "low_face_quality",
                "extreme_pose"
              ]
            },
            {
              "type": "null"
            }
          ]
        },
        "decided_by": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "model",
                "user",
                "rule"
              ]
            },
            {
              "type": "null"
            }
          ]
        },
        "decided_at": {
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "allOf": [
        {
          "$comment": "Machine-checked half of the precision invariant: the two assignment states that can never be eligible. The positive direction (eligible implies confidence >= threshold) is asserted in the golden tests, since JSON Schema cannot compare two sibling values.",
          "if": {
            "properties": {
              "assignment": {
                "enum": [
                  "unassigned",
                  "user_rejected",
                  "auto_below_threshold",
                  "review_queued",
                  "ambiguous_multiple_candidates"
                ]
              }
            },
            "required": [
              "assignment"
            ]
          },
          "then": {
            "properties": {
              "eligible_for_automated_output": {
                "const": false
              }
            }
          }
        },
        {
          "$comment": "An eligible face must name a person and must state the confidence and threshold that justified it. No unexplained permissions.",
          "if": {
            "properties": {
              "eligible_for_automated_output": {
                "const": true
              }
            },
            "required": [
              "eligible_for_automated_output"
            ]
          },
          "then": {
            "required": [
              "person_id",
              "confidence",
              "threshold_used"
            ],
            "properties": {
              "person_id": {
                "$ref": "common.schema.json#/$defs/Uuid"
              },
              "confidence": {
                "$ref": "common.schema.json#/$defs/Confidence"
              },
              "threshold_used": {
                "$ref": "common.schema.json#/$defs/Confidence"
              }
            }
          }
        }
      ]
    },
    "SensitiveFlags": {
      "title": "SensitiveFlags",
      "description": "Child-face labeling sits behind separate explicit consent (build plan section 8). Modelled here rather than on the person so that the gate is evaluated at the point of use.",
      "$comment": "This block is REQUIRED on every FaceRecord. Making it optional would let a face become eligible for unattended output without anyone having decided whether it belongs to a child, and 'nobody asked' is not the same as 'no'. `unknown` is the conservative default and is explicitly NOT treated as adult.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "minor_status"
      ],
      "properties": {
        "minor_status": {
          "description": "`unknown` is the default and is NOT treated as adult. Estimated age is a signal for asking the user, never a licence to proceed.",
          "type": "string",
          "enum": [
            "unknown",
            "estimated_minor",
            "confirmed_minor",
            "confirmed_adult"
          ],
          "default": "unknown"
        },
        "labeling_consent": {
          "description": "Required before a confirmed_minor face may be labeled with a person identity. Absent consent, the face is still detected and counted, but never named. Must be scoped to minor_face_labeling specifically -- a consent granted for cloud rendering does not authorise naming a child.",
          "oneOf": [
            {
              "$ref": "common.schema.json#/$defs/ConsentRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "excluded_from_sharing": {
          "type": "boolean",
          "default": false
        }
      },
      "allOf": [
        {
          "$comment": "A consent attached here may only ever be the child-labeling one, and it must be live. A revoked consent that still validates is worse than no consent at all, because it reads as permission.",
          "if": {
            "properties": {
              "labeling_consent": {
                "type": "object"
              }
            },
            "required": [
              "labeling_consent"
            ]
          },
          "then": {
            "properties": {
              "labeling_consent": {
                "type": "object",
                "properties": {
                  "scope": {
                    "const": "minor_face_labeling"
                  },
                  "revoked_at": {
                    "type": "null"
                  }
                }
              }
            }
          }
        }
      ]
    },
    "FaceTrack": {
      "title": "FaceTrack",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "track_id",
        "track_range"
      ],
      "properties": {
        "track_id": {
          "$ref": "common.schema.json#/$defs/Uuid"
        },
        "track_range": {
          "description": "Source-time span the track covers.",
          "$ref": "common.schema.json#/$defs/TimeRange"
        },
        "position_in_track": {
          "oneOf": [
            {
              "type": "integer",
              "minimum": 0
            },
            {
              "type": "null"
            }
          ]
        },
        "track_length": {
          "oneOf": [
            {
              "type": "integer",
              "minimum": 1
            },
            {
              "type": "null"
            }
          ]
        },
        "is_track_representative": {
          "description": "The single best frame of this track, chosen by face quality. Identity is decided once per track from the representative, not voted per frame -- 120 correlated votes are not 120 pieces of evidence.",
          "type": "boolean",
          "default": false
        }
      }
    }
  }
}
