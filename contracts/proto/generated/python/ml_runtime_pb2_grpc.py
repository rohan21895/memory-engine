# GENERATED FILE -- DO NOT EDIT.
#
# Produced by contracts/proto/generate.py from contracts/proto/ml_runtime.proto
# with grpcio-tools==1.68.1. Edit the .proto and re-run `npm run codegen`.
# CI fails if these files drift from the proto.

# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from . import ml_runtime_pb2 as ml__runtime__pb2

GRPC_GENERATED_VERSION = '1.68.1'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + f' but the generated code in ml_runtime_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class MlRuntimeStub(object):
    """The local gRPC interface between the intelligence packages (Claude) and the
    model-hosting process (Codex). Loopback only; this service never listens on a
    routable interface.

    FOUR PROPERTIES THIS CONTRACT ENFORCES STRUCTURALLY, NOT BY CONVENTION

    1. ANALYSIS NEVER TOUCHES ORIGINAL MEDIA.
    There is deliberately no field anywhere in this file that accepts a
    filesystem path, a URL, or an original media id. An InferRequest carries
    either a proxy id -- which the host resolves through media-db against the
    proxy store only -- or an already-decoded tensor. A caller cannot ask this
    service to open a source file, because the request type has nowhere to put
    one. Source files are opened exactly twice in their life: at proxy
    generation and at final render, and neither goes through here.

    2. EVERY RESULT IS PINNED TO THE WEIGHTS THAT PRODUCED IT.
    InferResponse carries the ModelPin actually used, including the weights
    hash. A caller that requested a model version and silently got another can
    detect it. This is what makes "why is this photo ranked 0.82" answerable
    after a model swap.

    3. ERRORS SAY WHETHER RETRYING COULD HELP.
    A JobSpec is resumable and every step idempotent, so the scheduler needs to
    distinguish "GPU was busy" from "these weights are unlicensed". Retrying a
    terminal error forever is how a queue dies quietly.

    4. REQUESTS CARRY DEADLINES AND ARE CANCELLABLE.
    A 300k-file scan must stay interruptible. Deadlines are expressed in the
    request as well as in gRPC metadata so the host can reject work it cannot
    finish rather than starting it.

    ---------------------------------------------------------------- service ---

    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.ListModels = channel.unary_unary(
                '/memory_engine.ml_runtime.v0.MlRuntime/ListModels',
                request_serializer=ml__runtime__pb2.ListModelsRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.ListModelsResponse.FromString,
                _registered_method=True)
        self.Health = channel.unary_unary(
                '/memory_engine.ml_runtime.v0.MlRuntime/Health',
                request_serializer=ml__runtime__pb2.HealthRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.HealthResponse.FromString,
                _registered_method=True)
        self.Infer = channel.unary_unary(
                '/memory_engine.ml_runtime.v0.MlRuntime/Infer',
                request_serializer=ml__runtime__pb2.InferRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.InferResponse.FromString,
                _registered_method=True)
        self.InferStream = channel.stream_stream(
                '/memory_engine.ml_runtime.v0.MlRuntime/InferStream',
                request_serializer=ml__runtime__pb2.InferRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.InferResponse.FromString,
                _registered_method=True)
        self.LoadModel = channel.unary_unary(
                '/memory_engine.ml_runtime.v0.MlRuntime/LoadModel',
                request_serializer=ml__runtime__pb2.LoadModelRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.LoadModelResponse.FromString,
                _registered_method=True)
        self.UnloadModel = channel.unary_unary(
                '/memory_engine.ml_runtime.v0.MlRuntime/UnloadModel',
                request_serializer=ml__runtime__pb2.UnloadModelRequest.SerializeToString,
                response_deserializer=ml__runtime__pb2.UnloadModelResponse.FromString,
                _registered_method=True)


class MlRuntimeServicer(object):
    """The local gRPC interface between the intelligence packages (Claude) and the
    model-hosting process (Codex). Loopback only; this service never listens on a
    routable interface.

    FOUR PROPERTIES THIS CONTRACT ENFORCES STRUCTURALLY, NOT BY CONVENTION

    1. ANALYSIS NEVER TOUCHES ORIGINAL MEDIA.
    There is deliberately no field anywhere in this file that accepts a
    filesystem path, a URL, or an original media id. An InferRequest carries
    either a proxy id -- which the host resolves through media-db against the
    proxy store only -- or an already-decoded tensor. A caller cannot ask this
    service to open a source file, because the request type has nowhere to put
    one. Source files are opened exactly twice in their life: at proxy
    generation and at final render, and neither goes through here.

    2. EVERY RESULT IS PINNED TO THE WEIGHTS THAT PRODUCED IT.
    InferResponse carries the ModelPin actually used, including the weights
    hash. A caller that requested a model version and silently got another can
    detect it. This is what makes "why is this photo ranked 0.82" answerable
    after a model swap.

    3. ERRORS SAY WHETHER RETRYING COULD HELP.
    A JobSpec is resumable and every step idempotent, so the scheduler needs to
    distinguish "GPU was busy" from "these weights are unlicensed". Retrying a
    terminal error forever is how a queue dies quietly.

    4. REQUESTS CARRY DEADLINES AND ARE CANCELLABLE.
    A 300k-file scan must stay interruptible. Deadlines are expressed in the
    request as well as in gRPC metadata so the host can reject work it cannot
    finish rather than starting it.

    ---------------------------------------------------------------- service ---

    """

    def ListModels(self, request, context):
        """What can this host load right now, and why not the rest.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def Health(self, request, context):
        """Liveness plus loaded-model state. Cheap enough to poll.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def Infer(self, request, context):
        """Batched inference. One request, one response, N items.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def InferStream(self, request_iterator, context):
        """Streaming variant for long sweeps: results arrive per batch so a 200-hour
        library does not accumulate a single enormous response, and cancelling the
        stream cancels the work.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def LoadModel(self, request, context):
        """Load or evict explicitly. The host may also load lazily on first Infer;
        this exists so a scan can warm the models it is about to hammer.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def UnloadModel(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_MlRuntimeServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'ListModels': grpc.unary_unary_rpc_method_handler(
                    servicer.ListModels,
                    request_deserializer=ml__runtime__pb2.ListModelsRequest.FromString,
                    response_serializer=ml__runtime__pb2.ListModelsResponse.SerializeToString,
            ),
            'Health': grpc.unary_unary_rpc_method_handler(
                    servicer.Health,
                    request_deserializer=ml__runtime__pb2.HealthRequest.FromString,
                    response_serializer=ml__runtime__pb2.HealthResponse.SerializeToString,
            ),
            'Infer': grpc.unary_unary_rpc_method_handler(
                    servicer.Infer,
                    request_deserializer=ml__runtime__pb2.InferRequest.FromString,
                    response_serializer=ml__runtime__pb2.InferResponse.SerializeToString,
            ),
            'InferStream': grpc.stream_stream_rpc_method_handler(
                    servicer.InferStream,
                    request_deserializer=ml__runtime__pb2.InferRequest.FromString,
                    response_serializer=ml__runtime__pb2.InferResponse.SerializeToString,
            ),
            'LoadModel': grpc.unary_unary_rpc_method_handler(
                    servicer.LoadModel,
                    request_deserializer=ml__runtime__pb2.LoadModelRequest.FromString,
                    response_serializer=ml__runtime__pb2.LoadModelResponse.SerializeToString,
            ),
            'UnloadModel': grpc.unary_unary_rpc_method_handler(
                    servicer.UnloadModel,
                    request_deserializer=ml__runtime__pb2.UnloadModelRequest.FromString,
                    response_serializer=ml__runtime__pb2.UnloadModelResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'memory_engine.ml_runtime.v0.MlRuntime', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('memory_engine.ml_runtime.v0.MlRuntime', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class MlRuntime(object):
    """The local gRPC interface between the intelligence packages (Claude) and the
    model-hosting process (Codex). Loopback only; this service never listens on a
    routable interface.

    FOUR PROPERTIES THIS CONTRACT ENFORCES STRUCTURALLY, NOT BY CONVENTION

    1. ANALYSIS NEVER TOUCHES ORIGINAL MEDIA.
    There is deliberately no field anywhere in this file that accepts a
    filesystem path, a URL, or an original media id. An InferRequest carries
    either a proxy id -- which the host resolves through media-db against the
    proxy store only -- or an already-decoded tensor. A caller cannot ask this
    service to open a source file, because the request type has nowhere to put
    one. Source files are opened exactly twice in their life: at proxy
    generation and at final render, and neither goes through here.

    2. EVERY RESULT IS PINNED TO THE WEIGHTS THAT PRODUCED IT.
    InferResponse carries the ModelPin actually used, including the weights
    hash. A caller that requested a model version and silently got another can
    detect it. This is what makes "why is this photo ranked 0.82" answerable
    after a model swap.

    3. ERRORS SAY WHETHER RETRYING COULD HELP.
    A JobSpec is resumable and every step idempotent, so the scheduler needs to
    distinguish "GPU was busy" from "these weights are unlicensed". Retrying a
    terminal error forever is how a queue dies quietly.

    4. REQUESTS CARRY DEADLINES AND ARE CANCELLABLE.
    A 300k-file scan must stay interruptible. Deadlines are expressed in the
    request as well as in gRPC metadata so the host can reject work it cannot
    finish rather than starting it.

    ---------------------------------------------------------------- service ---

    """

    @staticmethod
    def ListModels(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/ListModels',
            ml__runtime__pb2.ListModelsRequest.SerializeToString,
            ml__runtime__pb2.ListModelsResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def Health(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/Health',
            ml__runtime__pb2.HealthRequest.SerializeToString,
            ml__runtime__pb2.HealthResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def Infer(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/Infer',
            ml__runtime__pb2.InferRequest.SerializeToString,
            ml__runtime__pb2.InferResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def InferStream(request_iterator,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.stream_stream(
            request_iterator,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/InferStream',
            ml__runtime__pb2.InferRequest.SerializeToString,
            ml__runtime__pb2.InferResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def LoadModel(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/LoadModel',
            ml__runtime__pb2.LoadModelRequest.SerializeToString,
            ml__runtime__pb2.LoadModelResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def UnloadModel(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memory_engine.ml_runtime.v0.MlRuntime/UnloadModel',
            ml__runtime__pb2.UnloadModelRequest.SerializeToString,
            ml__runtime__pb2.UnloadModelResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
